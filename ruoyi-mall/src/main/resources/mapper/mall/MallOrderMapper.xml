<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.mall.mapper.MallOrderMapper">
    
    <resultMap type="com.ruoyi.mall.domain.MallOrder" id="MallOrderResult">
        <id     property="orderId"         column="order_id"         />
        <result property="orderNo"         column="order_no"         />
        <result property="userId"          column="user_id"          />
        <result property="totalAmount"     column="total_amount"     />
        <result property="payStatus"       column="pay_status"       />
        <result property="orderStatus"     column="order_status"     />
        <result property="receiverName"    column="receiver_name"    />
        <result property="receiverPhone"   column="receiver_phone"   />
        <result property="receiverAddress" column="receiver_address" />
        <result property="createTime"      column="create_time"      />
        <result property="userName"        column="user_name"        />
    </resultMap>

    <sql id="selectMallOrderVo">
        select o.order_id, o.order_no, o.user_id, o.total_amount, o.pay_status, 
               o.order_status, o.receiver_name, o.receiver_phone, o.receiver_address, 
               o.create_time, u.user_name
        from mall_order o
        left join sys_user u on o.user_id = u.user_id
    </sql>

    <select id="selectMallOrderList" parameterType="com.ruoyi.mall.domain.MallOrder" resultMap="MallOrderResult">
        <include refid="selectMallOrderVo"/>
        <where>
            <if test="orderNo != null and orderNo != ''">
                AND o.order_no like concat('%', #{orderNo}, '%')
            </if>
            <if test="userId != null">
                AND o.user_id = #{userId}
            </if>
            <if test="payStatus != null and payStatus != ''">
                AND o.pay_status = #{payStatus}
            </if>
            <if test="orderStatus != null and orderStatus != ''">
                AND o.order_status = #{orderStatus}
            </if>
            <if test="receiverName != null and receiverName != ''">
                AND o.receiver_name like concat('%', #{receiverName}, '%')
            </if>
            <if test="receiverPhone != null and receiverPhone != ''">
                AND o.receiver_phone like concat('%', #{receiverPhone}, '%')
            </if>
        </where>
        order by o.create_time desc
    </select>
    
    <select id="selectMallOrderById" parameterType="Long" resultMap="MallOrderResult">
        <include refid="selectMallOrderVo"/>
        where o.order_id = #{orderId}
    </select>

    <select id="selectMallOrderByOrderNo" parameterType="String" resultMap="MallOrderResult">
        <include refid="selectMallOrderVo"/>
        where o.order_no = #{orderNo}
    </select>

    <select id="selectMallOrderByUserId" parameterType="Long" resultMap="MallOrderResult">
        <include refid="selectMallOrderVo"/>
        where o.user_id = #{userId}
        order by o.create_time desc
    </select>
        
    <insert id="insertMallOrder" parameterType="com.ruoyi.mall.domain.MallOrder" useGeneratedKeys="true" keyProperty="orderId">
        insert into mall_order
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="orderNo != null and orderNo != ''">order_no,</if>
            <if test="userId != null">user_id,</if>
            <if test="totalAmount != null">total_amount,</if>
            <if test="payStatus != null">pay_status,</if>
            <if test="orderStatus != null">order_status,</if>
            <if test="receiverName != null">receiver_name,</if>
            <if test="receiverPhone != null">receiver_phone,</if>
            <if test="receiverAddress != null">receiver_address,</if>
            <if test="createTime != null">create_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="orderNo != null and orderNo != ''">#{orderNo},</if>
            <if test="userId != null">#{userId},</if>
            <if test="totalAmount != null">#{totalAmount},</if>
            <if test="payStatus != null">#{payStatus},</if>
            <if test="orderStatus != null">#{orderStatus},</if>
            <if test="receiverName != null">#{receiverName},</if>
            <if test="receiverPhone != null">#{receiverPhone},</if>
            <if test="receiverAddress != null">#{receiverAddress},</if>
            <if test="createTime != null">#{createTime},</if>
        </trim>
    </insert>

    <update id="updateMallOrder" parameterType="com.ruoyi.mall.domain.MallOrder">
        update mall_order
        <trim prefix="SET" suffixOverrides=",">
            <if test="orderNo != null and orderNo != ''">order_no = #{orderNo},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="totalAmount != null">total_amount = #{totalAmount},</if>
            <if test="payStatus != null">pay_status = #{payStatus},</if>
            <if test="orderStatus != null">order_status = #{orderStatus},</if>
            <if test="receiverName != null">receiver_name = #{receiverName},</if>
            <if test="receiverPhone != null">receiver_phone = #{receiverPhone},</if>
            <if test="receiverAddress != null">receiver_address = #{receiverAddress},</if>
        </trim>
        where order_id = #{orderId}
    </update>

    <delete id="deleteMallOrderById" parameterType="Long">
        delete from mall_order where order_id = #{orderId}
    </delete>

    <delete id="deleteMallOrderByIds" parameterType="String">
        delete from mall_order where order_id in 
        <foreach item="orderId" collection="array" open="(" separator="," close=")">
            #{orderId}
        </foreach>
    </delete>
</mapper>
